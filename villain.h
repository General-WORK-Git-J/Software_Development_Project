#pragma once 

#include <iostream>
#include <string>
#include <vector>

using std::string;
using std::cout;
using std::endl;
using std::vector;


class Villain {

private:
    
    int bounty;
    int dangerLevel;
    string weakness;

public:

    Villain();
    Villain(int b, int d, string w) : bounty(b), dangerLevel(d), weakness(w) {}










};
